// shared trait for all Prompts
use crate::error::WormTongueError;
use crate::tongues::common::PromptType;

//pub trait LLMPrompt {
//fn prompt_type(&self) -> PromptType;

//fn add_message<T>(&mut self, message: &T) -> Result<(), WormTongueError>;
//}
